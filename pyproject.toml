[project]
name = "hiver-support-agent"
version = "0.1.0"
description = "Auditable AppleSupport retrieval agent and honest evaluation workflow"
requires-python = ">=3.10"
dependencies = []

[tool.ruff]
line-length = 110
